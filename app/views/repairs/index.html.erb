<h1>All repairs</h1>

<% @repairs.each do |repair| %>
  <p>
    <%= repair.issue_type.humanize %> - <%= link_to 'Show', repair_path(repair) %> <%= link_to 'Edit', edit_repair_path(repair) %>
  </p>
<% end %>
